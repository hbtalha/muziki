enum SwipeDirection { right, left }

enum PlaylistSorting {
  byTitle,
  byFilename,
  byAlbumm,
  byArtist,
  byAlbumArtist,
  byTrackNumber,
  byDuration,
  byYear,
  randomly,
}

enum AlbumsSorting {
  byName,
  byNumberOfSongs,
  byDuration,
  byYear,
  byAlbumArtist,
}

enum SortingOrder { ascending, descending }
